# Rough Idea — Tmux Agent Sessions

I would like to have the agents run inside tmux by default. Whether it's tmux, cmux, whatever. This is to improve the observability story, so that when `bm team start` is run, later on I can attach to tmux, and find each agent is running in a new window — properly named, etc.
