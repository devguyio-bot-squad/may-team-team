# Code Task 02 — Fix documentation contradictions

## Objective

Correct documentation that contradicts the actual codebase behavior. The team CLAUDE.md claims context files are symlinks (they're copies), claims propagation is automatic (it requires `bm teams sync`), and the workspace-sync SKILL.md has an incorrect transformation rule. Also remove all references to member branches (eliminated by Task 01) from documentation.

## Traceability

| Type | IDs |
|------|-----|
| Acceptance Criteria | AC-03, AC-04, AC-05 |

## Scope

### Files to modify

**`team/CLAUDE.md` — Propagation Model table:**

Search for: `"workspace files are symlinks into"`

Current (incorrect):
```
| Member PROMPT.md, CLAUDE.md | Auto — workspace files are symlinks into `team/` |
```
Fix to:
```
| Member PROMPT.md, CLAUDE.md | **Manual** — requires `bm teams sync` (re-copy when source is newer) |
```

The "Auto — agent pulls `team/` every scan" claim for knowledge/invariants/PROCESS.md is partially accurate (board-scanner does `git -C team pull --ff-only`) but the team CLAUDE.md is NOT auto-propagated — it's a copy that requires sync. Clarify this.

**`team/CLAUDE.md` — Workspace Model diagram:**

Search for: arrow notation `→ team/members/`

Current (incorrect, uses arrow notation suggesting symlinks):
```
  PROMPT.md → team/members/{{member_dir}}/PROMPT.md
  CLAUDE.md → team/members/{{member_dir}}/CLAUDE.md
```
Fix to remove arrow notation and show these are copies:
```
  PROMPT.md                      # Copy from team/members/{{member_dir}}/
  CLAUDE.md                      # Copy from team/members/{{member_dir}}/
```

Also remove the note "Pulling `team/` updates all team configuration" which is misleading — only knowledge/invariants/PROCESS.md update via pull; copies require sync.

**`team/members/chief-of-staff-kevin/coding-agent/skills/workspace-sync/SKILL.md` — Rule 2:**

Search for: `"CLAUDE.md → CLAUDE.md Rename"` or `"CLAUDE.md → CLAUDE.md"`

Current (incorrect — describes a no-op):
```
### 2. CLAUDE.md → CLAUDE.md Rename
Profile uses `CLAUDE.md`. Team repo uses `CLAUDE.md`. When copying any file
named `CLAUDE.md` from the profile, rename it to `CLAUDE.md`.
```
Fix to reflect reality:
```
### 2. context.md → CLAUDE.md Rename
Profile uses `context.md` as the context filename. The team repo's `coding_agent.context_file`
config (in `botminter.yml`) specifies the target filename (e.g., `CLAUDE.md`). When copying
context files from profile to team repo, rename `context.md` to the configured `context_file` value.
```

**MkDocs documentation (botminter project):**

These files describe member branches as current behavior — must be updated after Task 01 removes them:

- `docs/content/concepts/workspace-model.md` — Remove paragraph about member branches (search for "member branch"), remove sync steps mentioning "Checkout member branch"
- `docs/content/reference/cli.md` — Remove "checks out member branches" from `teams sync` description (search for "member branches")
- `docs/content/how-to/launch-members.md` — Remove step "Checks out member branches in all submodules" (search for "member branches")

**`projects/botminter/CLAUDE.md` — Workspace Model section:**

Search for: `"Member branches are checked out"` — remove this statement since member branches no longer exist.

**Profile template source files (botminter project — root cause):**

These are the SOURCE templates from which team repo docs are generated. Without fixing them, the next `apply-profile` operation would reintroduce the contradictions. Do NOT rename `context.md` files — only fix content claims:

- `profiles/agentic-sdlc-planning/context.md` — Fix propagation model table (search for "symlinks into") and workspace model diagram (search for arrow notation `→ team/members/`)
- `profiles/scrum/context.md` — Fix same patterns
- `profiles/scrum/roles/human-assistant/context.md` — Fix arrow notation
- `profiles/scrum/roles/chief-of-staff/context.md` — Check and fix if same issues exist

### What NOT to change

- Do NOT rename profile template files from `context.md` to `CLAUDE.md` — the extraction code handles the rename via `context_file` config. Only fix the documentation content within these files.
- The workspace-sync SKILL.md three-layer model diagram already correctly shows "(copied)" and "(symlinked)" — no changes needed there.

## Acceptance Criteria

- AC-03: `team/CLAUDE.md` propagation model table says "copies" (not "symlinks") and says "Manual — requires bm teams sync" for PROMPT.md and CLAUDE.md
- AC-04: `team/CLAUDE.md` workspace model diagram does not use `→` arrow notation for copied files; instead clearly shows these are copies
- AC-05: Workspace-sync SKILL.md transformation Rule 2 accurately describes `context.md → CLAUDE.md` rename via `context_file` config (not the current no-op "CLAUDE.md → CLAUDE.md")
- No documentation references to "member branches" remain in workspace-model.md, cli.md, launch-members.md, or projects/botminter/CLAUDE.md after Task 01 eliminates the concept

## Complexity

Low — documentation-only changes with clear before/after states. Multiple files but all follow the same search-and-replace pattern.

## Required Skills

- Markdown editing
- Understanding of BotMinter workspace model (copies vs symlinks, sync vs auto-propagation)
